PORT=5050
MONGO_URI=mongodb://127.0.0.1:27017/journeyfly
JWT_SECRET=journeyfly_super_secret_2026

# Optional: Email Service config (falls back to console log if blank)
EMAIL_HOST=smtp.mailtrap.io
EMAIL_PORT=2525
EMAIL_USER=
EMAIL_PASS=
EMAIL_FROM=noreply@journeyfly.com

# Optional: Weather API Key (falls back to synthetic data if blank)
OPENWEATHER_API_KEY=

# Optional: Cloudinary config (falls back to local uploads/ if blank)
CLOUDINARY_URL=

# Optional: Google Gemini API Key for AI Chatbot (falls back to smart pre-written responses if blank)
GEMINI_API_KEY=